FROM node:18-alpine

WORKDIR /app

# 安裝基本依賴
RUN apk add --no-cache \
    python3 \
    make \
    g++

# 複製package files
COPY package*.json ./

# 安裝依賴
RUN npm install

# 複製源代碼
COPY . .

# 暴露端口
EXPOSE 3000

# 開發模式啟動
CMD ["npm", "run", "dev"]